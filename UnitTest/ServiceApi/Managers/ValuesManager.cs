namespace ServiceApi.Managers;

public class ValuesManager
{

    public decimal CalculatePrice(decimal productPrice, decimal productCount, decimal discount)
    {
        if (discount > 100)
        {
            throw new 
        }
    }

}